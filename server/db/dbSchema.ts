// Gifts database set-up:

// CREATE TABLE gifts (
//   gift_id SERIAL PRIMARY KEY,
//     receiver     VARCHAR(50),
//   receiver_name      VARCHAR(50),
//   occasion     VARCHAR(50),
//   date      DATE,
//   interest      VARCHAR(50),
//   budget    VARCHAR(50),
//   gift       VARCHAR(50),
//   gifter_id  SERIAL,
//   FOREIGN KEY (gifter_id) REFERENCES users(user_id)
//   );

// User database set-up:

// CREATE TABLE users (
//   user_id     SERIAL PRIMARY KEY,
//   first_name     VARCHAR(50),
//   last_name      VARCHAR(50),
//   email     VARCHAR(50),
//   password       VARCHAR(50)
// );
